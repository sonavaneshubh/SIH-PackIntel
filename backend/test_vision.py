import asyncio, logging; logging.basicConfig(level=logging.INFO); from app.services.vision_service import GeminiProvider; from app.schemas.vision import VisionExtractionResult; p = GeminiProvider(); result = asyncio.run(p.extract('test_product.jpg')); print('SUCCESS:', result.success); print('ERROR:', result.error); print('SOURCE:', result.extraction_source); print('FIELDS:', result.fields)
